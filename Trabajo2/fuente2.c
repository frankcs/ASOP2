//este archivo es el fichero fuente que al compilarse produce el ejecutable Ej2
#include <stdio.h>
int main( ) {

  char str[100];

   printf( "Enter a value :");
   gets( str );

   printf( "\nYou entered: ");
   puts( str );

   return 0;
}